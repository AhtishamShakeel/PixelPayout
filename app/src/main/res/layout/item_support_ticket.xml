<?xml version="1.0" encoding="utf-8"?>
<!-- A row on My tickets. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ticketCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="8dp"
    android:background="@drawable/bg_summary_card"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:orientation="vertical"
    android:padding="14dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/ticketStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_status_pill"
            android:fontFamily="@font/jakarta_semibold"
            android:paddingStart="9dp"
            android:paddingTop="3dp"
            android:paddingEnd="9dp"
            android:paddingBottom="3dp"
            android:textSize="11sp"
            tools:text="Support replied" />

        <TextView
            android:id="@+id/ticketCategory"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:fontFamily="@font/jakarta_medium"
            android:maxLines="1"
            android:textColor="@color/text_ghost"
            android:textSize="11.5sp"
            tools:text="Redemption / order" />

        <View
            android:id="@+id/ticketUnread"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/bg_unread_dot" />

        <TextView
            android:id="@+id/ticketTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:fontFamily="@font/jakarta_medium"
            android:textColor="@color/text_ghost"
            android:textSize="11sp"
            tools:text="2h ago" />
    </LinearLayout>

    <TextView
        android:id="@+id/ticketSubject"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:fontFamily="@font/jakarta_semibold"
        android:maxLines="1"
        android:textColor="@color/text_strong"
        android:textSize="14sp"
        tools:text="My 325 UC order has not arrived yet" />

    <TextView
        android:id="@+id/ticketPreview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:ellipsize="end"
        android:fontFamily="@font/jakarta_medium"
        android:maxLines="2"
        android:textColor="@color/text_faint"
        android:textSize="12.5sp"
        tools:text="Support: We have resent your UC." />

</LinearLayout>
