<?xml version="1.0" encoding="utf-8"?>
<!-- "Watch ad · +1 play / quiz" on the Play tab. Filled violet with a light
     top-edge hairline so it reads as a button rather than the tinted chip it
     used to share with labels; the ripple is the press feedback. The
     disabled look (no ad ready) is the 0.5 alpha set in code. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
  android:color="#33FFFFFF">
  <item>
    <shape android:shape="rectangle">
      <gradient
        android:angle="270"
        android:endColor="@color/brand_violet_deep"
        android:startColor="@color/brand_violet" />
      <stroke
        android:width="1dp"
        android:color="#40FFFFFF" />
      <corners android:radius="10dp" />
    </shape>
  </item>
</ripple>
