<?xml version="1.0" encoding="utf-8"?>
<!-- The status dot on the boost card. The design uses a dot rather than an
     icon here: the card is a state readout, not an action. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/boost_blue" />
</shape>
