<?xml version="1.0" encoding="utf-8"?>
<!-- The caller's own row, picked out of thirty near-identical ones. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/violet_tint_16" />
    <stroke android:width="1dp" android:color="@color/violet_tint_50" />
    <corners android:radius="16dp" />
</shape>
