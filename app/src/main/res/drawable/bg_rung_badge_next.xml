<?xml version="1.0" encoding="utf-8"?><!--
    The number square on the next level up. Same tint as bg_chip_violet with
    the accent border added, so it reads as the one being climbed toward.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/violet_tint_20" />
    <stroke android:width="1dp" android:color="@color/brand_violet" />
    <corners android:radius="8dp" />
</shape>
