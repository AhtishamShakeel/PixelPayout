<?xml version="1.0" encoding="utf-8"?>
<!--
    "WHY" on a decline. Gold rather than red: the stars came back, so this is
    a refund with an explanation, not an error.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/gold_tint_16" />
    <stroke android:width="1dp" android:color="@color/gold_stroke" />
    <corners android:radius="8dp" />
</shape>
