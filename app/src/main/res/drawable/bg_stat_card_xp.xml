<?xml version="1.0" encoding="utf-8"?><!--
    The XP half of the pair. Identical to bg_stat_card_stars but for the
    border colour, which is the entire point - see that file for why the
    accent moved out of the ground and into the hairline.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/surface_card" />
    <stroke android:width="1dp" android:color="@color/xp_stroke" />
    <corners android:radius="18dp" />
</shape>
