<?xml version="1.0" encoding="utf-8"?>
<!-- The initial well on a medalled row, in the same shape bg_row_avatar uses
     so the column does not shift for the first three places. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/silver_tint_35" />
    <stroke android:width="1dp" android:color="@color/silver_60" />
</shape>
