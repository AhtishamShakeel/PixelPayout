<?xml version="1.0" encoding="utf-8"?>
<!--
  The quiz results card. Its contents are view_reward_results, the same layout
  the game results panel includes, so the two screens cannot drift apart again.
  Only the card around it lives here.
-->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="24dp"
    app:cardBackgroundColor="@color/surface_card"
    app:cardCornerRadius="20dp"
    app:cardElevation="12dp">

    <include
        android:id="@+id/resultsContent"
        layout="@layout/view_reward_results" />

</androidx.cardview.widget.CardView>
