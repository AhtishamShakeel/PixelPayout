<?xml version="1.0" encoding="utf-8"?>
<!-- The 30dp initial circle on a standings row. Real avatars, when there are
     any, load into this same well with this same ring. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/surface_dark" />
    <stroke android:width="1dp" android:color="@color/stroke_avatar" />
</shape>
