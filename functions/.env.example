# Copy to functions/.env and fill in. The real .env is gitignored because this
# repository is public.
#
# Comma-separated list of Google accounts allowed to grant themselves the admin
# claim via the bootstrapAdmin callable (used by public/admin.html). Leave empty
# to disable admin bootstrapping entirely.
ADMIN_BOOTSTRAP_EMAILS=you@example.com
